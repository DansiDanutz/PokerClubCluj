{
  "commentTypes": [
    "logic",
    "syntax"
  ],
  "strictness": 2,
  "triggerOnUpdates": true,
  "statusCheck": true,
  "statusCommentsEnabled": true,
  "comment": "AI-generated review; verified tests, required checks, and human approval remain merge gates.",
  "instructions": "Review for exploitable security, authorization, correctness, data-integrity, concurrency, and production-readiness defects. Require file and line evidence. Avoid style-only comments and never treat generated prose as proof of testing.",
  "ignoreKeywords": "greptile-ignore",
  "ignorePatterns": "node_modules/**\n.next/**\ndist/**\nbuild/**\ncoverage/**\npublic/**",
  "summarySection": {
    "included": true,
    "collapsible": false,
    "defaultOpen": false
  },
  "issuesTableSection": {
    "included": true,
    "collapsible": false,
    "defaultOpen": false
  },
  "confidenceScoreSection": {
    "included": true,
    "collapsible": false,
    "defaultOpen": false
  },
  "customContext": {
    "rules": [
      {
        "scope": [
          "**/*"
        ],
        "rule": "EVIDENCE: report only actionable defects with file:line evidence, a concrete failure mode, and a concise remediation. Do not report formatting or preference-only issues."
      },
      {
        "scope": [
          "**/*"
        ],
        "rule": "AUTHZ: server-side mutations must derive identity from a verified session and re-check ownership or role. Flag trust in client-supplied identities, roles, tenant IDs, or resource ownership."
      },
      {
        "scope": [
          "**/*"
        ],
        "rule": "SECRETS: flag hardcoded credentials, private keys, tokens, cookies, secrets in logs, and server-only values exposed to clients. Example values must be unmistakable placeholders."
      },
      {
        "scope": [
          "**/*"
        ],
        "rule": "FAIL-CLOSED: authentication, authorization, internal tools, deployment gates, and security controls must deny by default when configuration is missing or invalid."
      },
      {
        "scope": [
          "**/*"
        ],
        "rule": "INPUT: validate and bound untrusted input at the boundary. Flag unsafe parsing, path traversal, injection, NaN/Infinity, unbounded payloads, and missing schema validation."
      },
      {
        "scope": [
          "**/*"
        ],
        "rule": "IDEMPOTENCY: webhooks, jobs, imports, billing, settlement, and retryable handlers must not duplicate irreversible effects when replayed."
      },
      {
        "scope": [
          "**/*"
        ],
        "rule": "CONCURRENCY: flag read-then-write races, missing transactions or locks, unsafe shared state, non-atomic ledger/balance changes, and duplicate worker execution."
      },
      {
        "scope": [
          "**/*"
        ],
        "rule": "DEPENDENCIES: flag unpinned executable downloads, unsafe install hooks, dependency-confusion exposure, and upgrades that violate runtime or lockfile contracts."
      },
      {
        "scope": [
          "**/*"
        ],
        "rule": "TESTS: behavior changes need regression coverage at the failure boundary. Flag tests that mock away the critical behavior or assertions that cannot fail for the defect."
      },
      {
        "scope": [
          "**/*.ts",
          "**/*.tsx",
          "**/*.js",
          "**/*.jsx"
        ],
        "rule": "WEB RUNTIME: keep server-only modules and secrets out of client bundles; flag unsafe dynamic execution, missing await/error handling, and route handlers that trust client state."
      },
      {
        "scope": [
          "**/api/**",
          "**/routes/**",
          "**/actions/**",
          "**/middleware.*"
        ],
        "rule": "WEB BOUNDARIES: authenticate before parsing privileged operations, enforce CSRF/origin controls where cookies authorize writes, validate request bodies, and return non-sensitive errors."
      },
      {
        "scope": [
          "**/*.html",
          "**/*.tsx",
          "**/*.jsx"
        ],
        "rule": "BROWSER SECURITY: flag XSS sinks, unsafe HTML injection, untrusted redirects, token storage in browser-accessible persistence, and links/windows missing appropriate isolation."
      },
      {
        "scope": [
          "vercel.json",
          "next.config.*",
          "vite.config.*",
          "**/Dockerfile",
          ".github/workflows/**"
        ],
        "rule": "DEPLOYMENT: flag public exposure of internal routes or artifacts, missing security headers, preview/production drift, unsafe environment fallbacks, and deployments not tied to the reviewed commit."
      },
      {
        "scope": [
          "src/app/api/petition/admin/**"
        ],
        "rule": "PETITION ADMIN: every feature, hide, flag, block, delete, and administrative listing operation must require verified administrator identity and authorization, use safe state transitions, resist CSRF/origin abuse, and produce non-sensitive audit evidence."
      },
      {
        "scope": [
          "src/app/api/petition/**",
          "src/app/memoriu/**"
        ],
        "rule": "PETITION PRIVACY: minimize signature/petitioner PII, establish consent and retention behavior, prevent duplicate or automated signatures, validate all fields, avoid public enumeration, and never log sensitive submissions."
      },
      {
        "scope": [
          "src/app/api/petition/**"
        ],
        "rule": "PETITION ABUSE: enforce bounded payloads, rate limits, idempotency, safe moderation state, and server-side ownership/role checks. Flag trust in client-provided admin, status, feature, block, or identity values."
      },
      {
        "scope": [
          "src/app/**"
        ],
        "rule": "PETITION CONTENT: flag stored or reflected XSS, unsafe HTML/PDF rendering, formula injection in exports, untrusted redirects, and server-only data or credentials crossing into client bundles."
      }
    ],
    "files": []
  }
}
